# Ensure EB/ALB health checks to '/' succeed even though the app is mounted at /api.
# Exact-match location has higher priority than the default "location /".
location = / {
    proxy_pass http://127.0.0.1:5000/api/health;
}
